// Code generated by go2ts. DO NOT EDIT.
// Source: predictions.go

export interface Prediction {
    id: string;
    created_at: string;
    name: string;
    description: string;
    status: PredictionStatus;
    closes_at: string;
    choices: PredictionChoice[];
    odds_visible_before_bet: boolean;
}

export interface PredictionChoice {
    id: string;
    name: string;
}

export interface PredictionOdds {
    total_tokens_placed: number;
    total_bets_placed: number;
    choices: PredictionChoiceOdds[];
}

export interface PredictionChoiceOdds {
    prediction_choice_id: string;
    tokens_placed: number;
    bets_placed: number;
    /** OddsBasisPoints is the payout multiplier in basis points (100 = 1x, 250 = 2.5x, 400 = 4x). */
    /** 0 means no bets have been placed on this choice. */
    odds_basis_points: number;
}

export interface Bet {
    id: string;
    created_at: string;
    user_id: string;
    prediction_id: string;
    prediction_choice_id: string;
    amount: number;
    status: BetStatus;
    won_amount: number;
}

export const PredictionStatus = {
  Open: "open",
  Closed: "closed",
  Decided: "decided",
  Void: "void",
} as const;

export type PredictionStatus = typeof PredictionStatus[keyof typeof PredictionStatus];


export const BetStatus = {
  Placed: "placed",
  Won: "won",
  Lost: "lost",
  Voided: "voided",
} as const;

export type BetStatus = typeof BetStatus[keyof typeof BetStatus];


