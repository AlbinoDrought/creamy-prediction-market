// Code generated by go2ts. DO NOT EDIT.
// Source: users.go

/** UserCosmetics represents the currently equipped cosmetic values for a user. */
export interface UserCosmetics {
    avatar_color: string;
    avatar_emoji: string;
    name_emoji: string;
    avatar_effect: string;
    name_effect: string;
    name_bold: boolean;
    name_font: string;
    title: string;
    hat: string;
    avatar_item: string;
}

export interface User {
    id: string;
    name: string;
    /** PINHash is a bcrypt hash of the user's pin. */
    /** This isn't meant to be secure at all really - we expect the pin to simply be four digits, like 0000. */
    pin_hash: number[];
    admin: boolean;
    tokens: number;
    spins: number;
    coins: number;
    owned_items: string[];
    cosmetics: UserCosmetics;
}

export interface LeaderboardUser {
    id: string;
    name: string;
    tokens: number;
    score: number;
    rank: number;
    achievements: string[];
    cosmetics: UserCosmetics;
}

export interface TokenLog {
    id: string;
    created_at: string;
    user_id: string;
    change: number;
    cause: TokenChangeCause;
    /** BetID and PredictionID are set if cause is TokenChangeCauseBetPlaced, TokenChangeCauseBetWon, or TokenChangeCauseBetVoided */
    bet_id: string;
    prediction_id: string;
}

export const TokenChangeCause = {
  Start: "start",
  BetPlaced: "bet-placed",
  BetWon: "bet-won",
  BetVoided: "bet-voided",
  Gift: "gift",
} as const;

export type TokenChangeCause = typeof TokenChangeCause[keyof typeof TokenChangeCause];


